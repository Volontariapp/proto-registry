name: Sync Protobuf to NPM
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  sync:
    uses: Volontariapp/ci-tools/.github/workflows/proto-sync.yml@main
    secrets: inherit
